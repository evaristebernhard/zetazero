import ZetaZero.HardyGaugeInvariantContourForm.PacketSameForm

/-!
Compatibility facade for packet-polarized boundary equality.

The full theorem `packetSameForm_boundaryIntegral_eq` and its contour nonzero
predicate now live in `PacketSameForm.lean`; this file remains as a stable import
path for parallel work that already referenced the more specific module name.
-/
