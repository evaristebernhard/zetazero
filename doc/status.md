# Formalization status

This table is the human-readable status ledger.  The Blueprint is the
machine-rendered dependency view.  Update both in the same change whenever a
major node changes state.

| ID | Status | Lean declarations linked | Notes |
|---|---|---:|---|
| M01 | Lean core complete; Blueprint certification pending | 1 | The physical-coordinate rank-plus-Hilbert--Schmidt min--max theorem builds with warnings-as-errors and resolves under `leanblueprint checkdecls`; `\leanok` plus the full Blueprint build are still pending, so M01 is not yet green by project policy. |
| M02 | In progress; global zero sum and Hardy-source bridge verified | 1 | The packet same-form contour, zero-free boundary integrability, and vendored weighted rectangle argument principle give the finite multiplicity-weighted `Z₁` zero sum. The normalization bridge to Zeta23 is now also verified: off the real axis `hardyF = L₂`, hence `Z₁ = hardyW`, with derivative, analytic-order, and zerohood transport. The packet factor now also has a checked reflected-adjoint identity `packetPolarization_hardyReflect_swap`; the actual contour orientation/curvature adjoint and finite `IsHermitian` matrix remain open. `npm test -- mainline-bridge` passes. The remaining M02 work is the global specialization of these residues to the stationary packet weights and the remaining good-height/log-derivative estimates. |
| M03 | Straightening/stationary dictionary verified; global packet specialization remains | 1 | Shared zero-count/reflection infrastructure and local stationary residue/inertia theory are in place. The straightening bridge now verifies that `s = 1/2 + iz` turns Hardy reflection into `z ↦ conj z`, identifies the critical line with real `z`, transports stationary zerohood, and supplies the simplicity dictionary. The finite evaluation-pullback interface now proves that a target negative-index bound transfers to `q ∘ E` when `q 0 = 0`; it does not assume global injectivity and does not yet prove packet surjectivity or exact `B+P` counting. The manuscript's right/left vertical reflection seam has also been repaired at matrix level: the conjugation in `iota(s)=1-conj(s)`, curvature/packet adjoints, and reversed orientation are kept explicitly, so the final `I_right + I_right^* + J_arch` decomposition no longer rests on a scalar rewrite. This sesquilinear contour-adjoint step is not yet a Lean theorem. Both `npm test -- straightening` and the stationary part of `mainline-bridge` pass. Remaining work is packet-evaluation surjectivity, specialization of the global zero sum to the residue blocks, the matrix-adjoint contour transport in Lean, and the Jensen/log-derivative good-height estimates. |
| M04 | Finite-window stationary main term verified quantitatively; packet/Perron transfer remains | 0 | Lean contains the frozen-source algebra, exact stationary geometry/normalization, local quadratic and nonstationary control, the oscillatory Gaussian tail `≤2/R`, Abel-damped Fresnel evaluation, and the symmetric Fresnel-window error `≤4/R`. `StationaryMainTermError.lean` now proves the exact stationary kernel is Borel measurable, unit-modulus, and interval-integrable on every finite window, then combines the cubic replacement and Fresnel truncation into `‖∫_{-R}^R exactKernel - √π(1+i)‖ ≤ (4/3)R^4/√t_* + 4/R`. Under `R^5≤√t_*` this sharpens to `≤16/(3R)`. The extended `stationary-oscillatory` gate and fresh `lean-all` pass. The safe-edge exact/model/direct-residual/self-freezing split and a dimension-free continuous-linear-map Fubini lift for the finite packet model are also verified. The manuscript repair now removes the self source holomorphically before the arithmetic principal reduction; Lean has not yet been retargeted to this log-only split. |
| M05 | In progress; manuscript principal route replaced by lagwise log-only bridge, Lean transfer remains | 1 | The Mangoldt hierarchy, exact coefficient identity `(k+1)α_{k+1}(n)=log(n)Λ_{k+1}(n)`, weighted Chebyshev `1/(m+1)` gain, support-to-degree bound, and uniform factorial square-sum estimate are in Lean. The finite resolvent expansion now has an explicit geometric tail bound, while the vendored zeta-pole Laurent estimate is exposed through a continuous removable normalization with value one. The manuscript branch `fix/lagwise-hlz-log-only` no longer uses the old lag-convolution-to-one-sided-multiplier identification: it keeps the exact lag `d=y-x`, uses the cutoff `ell <= X exp(-|d|)`, and applies the scalar HLZ diagonal uniformly at fixed lag. The exact right-edge source is split into the log-only principal source, an `O(L^-2)` direct residual, and an `f'` regular class. This revised packet/Perron bridge is a manuscript proof candidate only and is not yet a checked Lean interface. |
| M06 | Abstract Schur-floor foundation started; manuscript target changed to log-only floor | 0 | `PrimitiveSchurFloor.lean` exposes the reusable cusp-minus-Schur and relative-error floor interfaces. The repaired manuscript replaces the old self-plus-log profile by `G_log` and targets the certified paper bound `K_{G_log} >= 0.02 Q_triangle`; the existing Lean abstract constants were written for the earlier `0.06 -> 0.03` interface and have not yet been retargeted or used to certify the new concrete profile. M06 therefore remains non-green. |
| M07 | Concrete gcd/operator Gram foundation verified; arithmetic deformation awaits M05 | 0 | For a finite complex analysis map `W`, Lean formalizes the pullback Gram quadratic and its positivity. The gcd kernel now also has an exact totient-weighted Hilbert Gram decomposition and an operator-valued lift through arbitrary labelled linear maps. The actual divisor/Euler comparison and its quantitative two-sided deformation remain pending. |
| M08 | Abstract feature and fixed-edge rank bookkeeping started; analytic compression awaits M03/M06/M07 | 0 | Finite-feature and translated-pole jet rank bounds are joined by a coefficient-free fixed-edge theorem: a finite sum of operators landing in one fixed subspace costs only that subspace's dimension, not the number of summands. Concrete packet feature maps and the regular-remainder compression remain open. |
| M09 | Abstract retained-space bookkeeping started; analytic maps await M08 | 0 | The reusable kernel/intersection budget now proves `codim(ker M₁ ∩ ker M₂) ≤ rank M₁ + rank M₂` and the threefold retained-intersection codimension bound. The actual global/local mean maps and their asymptotic rank estimates still depend on M08. |
| M10 | Abstract perturbation/min--max glue and density-one normalization interface started | 1 | Perturbation stability `P ≥ cG`, `|Δ| ≤ εG` ⇒ `P+Δ ≥ (c-ε)G` is formalized, including the manuscript's `ε=c/4` floor, and is connected to the existing rank-plus-Hilbert--Schmidt min--max theorem. `density_one_critical_line` now proves the final ratio normalization from an explicit off-critical little-o input. The analytic `F→H` perturbation/zero-count transfer and insertion of the actual M09 retained space remain open. |

No major node is green yet.  M01 is already linked and passes `checkdecls`, but
project policy still requires `\leanok` certification and a complete Blueprint
build before the node can turn green.  The current log-only/lagwise manuscript
repair changes the intended M04--M06 analytic interfaces but does not itself
constitute Lean verification.  Existing Lean gate results predate this manuscript
route change and must not be read as certification of the new bridge or the new
`0.02` concrete floor.
