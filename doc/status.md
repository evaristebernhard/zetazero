# Formalization status

This table is the human-readable status ledger. The Blueprint is the
machine-rendered dependency view. Update both in the same change whenever a
major node changes state.

| ID | Status | Lean declarations linked | Notes |
|---|---|---:|---|
| M01 | Lean core complete; Blueprint certification pending | 1 | The physical-coordinate rank-plus-Hilbert--Schmidt min--max theorem builds with warnings-as-errors and resolves under `leanblueprint checkdecls`; `\leanok` plus the full Blueprint build are still pending, so M01 is not yet green by project policy. |
| M02 | In progress; global zero sum and Hardy-source bridge verified | 1 | The packet same-form contour, zero-free boundary integrability, and vendored weighted rectangle argument principle give the finite multiplicity-weighted `Z₁` zero sum. The normalization bridge to Zeta23 is also verified: off the real axis `hardyF = L₂`, hence `Z₁ = hardyW`, with derivative, analytic-order, and zerohood transport. The packet factor has a checked reflected-adjoint identity `packetPolarization_hardyReflect_swap`; the actual contour orientation/curvature adjoint and finite `IsHermitian` matrix remain open. `npm test -- mainline-bridge` passes. |
| M03 | Straightening/stationary dictionary verified; global packet specialization remains | 1 | Shared zero-count/reflection infrastructure and local stationary residue/inertia theory are in place. The straightening bridge verifies that `s = 1/2 + iz` turns Hardy reflection into `z ↦ conj z`, identifies the critical line with real `z`, transports stationary zerohood, and supplies the simplicity dictionary. Packet-evaluation surjectivity, specialization of the global zero sum to residue blocks, the matrix-adjoint contour transport in Lean, and the Jensen/log-derivative good-height estimates remain. |
| M04 | Finite-window stationary main term verified; manuscript source interface changed | 0 | Lean contains the frozen-source algebra, exact stationary geometry/normalization, local quadratic and nonstationary control, the oscillatory Gaussian tail, Abel-damped Fresnel evaluation, and the finite-window stationary error. The active manuscript eliminates the archimedean self block holomorphically and uses the exact source split `L₁ = H_log + ΔH_dir + f'/(f-P)` with `ΔH_dir = O(L^-2)`. The stationary main term retains the physical additive carrier `e(-e^d ξ)`, which is the key input to the M05 obstruction below. |
| M05 | **Blocked: fatal additive-twist gap in lagwise scalar-HLZ lift** | 1 | The structural replacement by scalar HLZ plus actual packet lag is **not a proved bridge**. After exact packet polarization and one-χ stationary phase, fixed lag `d=y-x` leaves the carrier factor `e(-e^d ξ)`; in the invariant diagonal the physical carrier is the integer `ell` (or the corresponding collapsed total carrier), so the arithmetic sum is additively twisted. The scalar HLZ Euler diagonal used in `06_scalar_hlz_core.tex` is untwisted and does not justify dropping this carrier-dependent phase. An internal rank-versus-floor test gives the same obstruction: the physical direct-carrier realization is `o(N_T)`-rank plus relative-HS `o(N_T)`, which cannot simultaneously equal a fixed-floor full-rank lag operator up to errors of the same size. See `doc/lagwise_hlz_gap_audit.md`. A new additively twisted HLZ/Estermann theorem or a different second-stationary mechanism would be required. |
| M06 | Downstream only; current concrete formula is wrong, small positive floor may be salvageable conditionally | 0 | `PrimitiveSchurFloor.lean` exposes reusable cusp-minus-Schur interfaces, but M06 is blocked by M05. Independently, direct coefficient extraction from the manuscript's own log-only profile shows that the current `sinh(2ρ)` closed form and the claimed `0.98 Q_triangle` floor are incorrect. The correct closed form contains `(2ρ²-4ρ+3) cosh(2ρ)` plus polynomial terms. A numerical audit gives a cusp-minus-Schur margin corresponding to about `0.03775 Q_triangle`; a rigorous conservative target `0.03 Q_triangle` appears feasible and would still support the endgame choice `c₀=0.02` **if M05 were repaired**. This concrete calculation is not Lean-certified. |
| M07 | Auxiliary divisor-Gram library; no longer on active manuscript dependency chain | 0 | The finite complex Gram pullback, totient-weighted gcd decomposition, and operator-valued divisor lift remain useful verified infrastructure. The revised invariant-source proof no longer introduces an outer divisor deformation, so M07 is no longer a dependency of M08 or of the current theorem path. Further arithmetic-transfer formalization may proceed independently. |
| M08 | Downstream analytic shell/remainder target; blocked by M05/M06 | 0 | The active manuscript has removed the macroscopic compressed-shift principal operator and the HLP exact--model residual from its claimed theorem route. The new target is the stable-core frame, logarithmic unitary, geometric exterior/terminal leakage, the absolute-lag two-variable primitive criterion, and the four log-only regular classes. The remainder ledger also needs an explicit exponent/diagonal-amplitude ledger: a large `A₀` controls shell summation but does not by itself make a single-shell normalized jump `o(1)`. Existing Lean fixed-edge abstractions do not certify these concrete analytic maps. |
| M09 | Abstract retained-space bookkeeping started; analytic maps await M08 | 0 | The reusable kernel/intersection budget proves the threefold retained-intersection codimension bound. The manuscript retained space is now just `V_core ∩ V_mean ∩ V_end`, with no critical-J AFE deletion, frozen-self deletion, or low-sideband principal-source deletion. The actual global/local mean maps and asymptotic rank estimates still need formalization. |
| M10 | Abstract perturbation/min--max glue and density-one normalization interface started | 1 | Perturbation stability and the rank-plus-Hilbert--Schmidt min--max theorem are formalized, and `density_one_critical_line` proves the final ratio normalization from an off-critical little-o input. The manuscript endgame is only conditional at present: the required fixed positive principal floor cannot be inserted until M05 is repaired and M06 is re-certified. The analytic `F→H` perturbation/zero-count transfer and insertion of the actual M09 retained space also remain open. |

No major node is green yet. M01 is already linked and passes `checkdecls`, but
project policy still requires `\leanok` certification and a complete Blueprint
build before the node can turn green.

The active manuscript has physically removed the superseded
frozen-HLP/common-output/compressed-shift source files, and that structural
cleanup remains useful. A subsequent mathematical audit, however, found that
the replacement lagwise scalar-HLZ proposition still drops the physical
additive stationary carrier. This is currently a fatal M05 gap, not a minor
formalization omission. The corrected downstream log-only Schur calculation
cannot repair that upstream mismatch. See `doc/lagwise_hlz_gap_audit.md` for the
full route-internal audit and possible repair directions.
